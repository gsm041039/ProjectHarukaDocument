# 16 — Workflow Weakness Review (post multi-beat pilot, honest)

| # | Observed evidence | Severity | Responsible owner | Minimal patch | Test required |
|---|---|---|---|---|---|
| 1 | Pilot safety (Run Mode) is still enforced only by the orchestrator's own text obeying itself — no mechanical gate stops a less careful execution from writing pilot output into `PROJECT_STATUS.md` etc. This is the same gap flagged after the single-Beat pilot; Patch F formalized the *rule* but not a *mechanism*. | **IMPORTANT** (repeat, unresolved) | `story-run-workspace-manager` (would need to own an actual write-gate) | Add a `PILOT_MODE` flag to `story-run-workspace-manager` that mechanically refuses writes to the 5 durable files + approved Act/Beat Sheet/CDL paths for the run's duration | Run a pilot without an explicit "don't touch durable state" instruction in the task text → verify a mechanical gate still blocks it |
| 2 | The visual-bible cross-check that caught the "0.3s mirror-lag / Stage 1 夢幻鏡" resonance (`03`) happened because this execution chose to look, not because any skill rule forces it. The prior pilot's self-critique (item B) proposed exactly this rule; it was not in this task's 7-item patch list and remains unapplied. | **IMPORTANT** (carried forward, not yet patched — out of this task's declared scope) | `.claude/skills/story-room/SKILL.md` (Beat Sheet entry-read list) | Add: "if a beat references a named visual/symbolic tier, check `06_visual_bible.md` for its definition" | A beat citing a named tier, with the visual-bible check deliberately skipped in one run vs performed in another → compare traceability quality |
| 3 | Current-level detection still relies on files already read earlier in the session (both pilots). A genuinely cold session's "read full outline + beat sheet before claiming layer state" is not yet a forced procedural step. | IMPORTANT (carried forward from single-Beat pilot's critique item A, not in this task's patch list) | `story-orchestrator` Progressive Mode step 2 | Add explicit "full read required before layer claim" | Fresh session, no prior reads → verify full reads occur first |
| 4 | Beat 0c's "arm-twitch vs eye-glow" micro-choice was resolved cleanly because a convenient pre-existing Scene Reference camera constraint happened to exist. The same resolution pattern (AUTO_RESOLVABLE via found constraint) may not generalize to an Act/beat with no prior scene-level draft — risk of mis-classifying a genuine fork as resolved when no such constraint exists. | MINOR | `story-orchestrator` decision-class guidance | Add a caution note: "resolving via found constraint requires the constraint to actually exist and be load-bearing — do not assume one" | A beat with a real unresolved "btd" and no Scene Reference at all → verify it correctly escalates instead of being force-resolved |
| 5 | Obligation Matrix (`05`) marked Theme/stance `NOT_RELEVANT_WITH_REASON` for Beat 0c, but the bullying/rescuer-complex trigger arguably is an early, minor instance of the "態度 vs 命運" thread. Nothing is lost (captured under Character-arc/Setup-payoff instead), but the Theme row may be mis-labeled. | MINOR | `story-room` Obligation Pass guidance | Add a note distinguishing "theme fully expressed/contested" (RELEVANT) from "theme quietly seeded" (also RELEVANT, lighter weight) vs true absence (NOT_RELEVANT) | Re-run Beat 0c's Theme/stance classification with the clarified definition, compare |
| 6 | `ACT_I_OUTLINE.md`'s own beat ordering lists Beat 0d ("E-01當晚") before Beat 1 ("E-01" itself) — a document-structure oddity noted in `08` but not investigated further (out of this pilot's scope to alter approved Outline content). | MINOR / content-quality, not a workflow defect | `story-outline-synthesis-gate` (if ever asked to audit Act I Outline structure) | none needed from this task — flag for a future outline-audit round, author to confirm it's a labeling artifact | N/A this task |
| 7 | Patch B's middle tier (`COST_SIGNAL_OPTIONAL`) was never exercised — all 4 pilot beats landed cleanly in REQUIRED or NOT_APPLICABLE. The classification rule is unverified for the ambiguous middle case. | MINOR | `consequence-driven-progression.md` | none needed yet — note as an open regression case | Beat that plausibly works either way (e.g. a beat with a soft internal realization but no hard external event) → verify OPTIONAL is actually usable and not just decorative |
| 8 | Downstream References currently live inline per beat; not tested at full-Act scale (13 beats) where cross-references could get numerous. | COSMETIC | `story-room` output format | none needed yet | Build a full Act's Beat Sheet and check whether Downstream References need their own index file |

## Not patched this task
Per §30: none of the above were fixed during this run unless doing so was required for the requested pilot to be valid. None was required — the pilot completed and produced valid, traceable output despite all 8 items above.

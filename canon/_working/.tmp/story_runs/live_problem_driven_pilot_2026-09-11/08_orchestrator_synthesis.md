# Orchestrator Synthesis — Part A

(Not "specialist PASS × 3 therefore ready" — inspecting the actual candidates directly.)

**What works together:** C1 (quiet domestic micro-insert after Beat 4.5) and C3 (recurring combat tic across Beat 2 and Beat 5) hit two different textures at three well-spaced points across Act I's runtime, using only carriers that already exist (Beat 2, Beat 5 — approved DRAFT) or were already earmarked-but-undesigned (the `[NC]` slot). Together they fix both the before-gap and the after-silence identified in the problem blueprint.

**What conflicts:** nothing between C1 and C3. C2 was the one candidate with a real tension (competence-mystery vs. identity-mystery blur) — dropped from the combination, not silently ignored.

**What is redundant:** none of the adopted candidates duplicate an existing beat's function; care flagged only that C3's visual tic must stay visually distinct from the unrelated P-A06 fatigue cost-signal cues (a production note, not a design conflict).

**What is missing:** the exact wording/blocking/animation spec for each micro-insert — correctly deferred. Designing that now would be Scene-layer work, and Scene layer is not authorized in real production (Round 196: `TRANSITION_ALLOWED = NO`, Sequence → Scene Architecture).

**Key trade-off:** slightly more future Scene-layer design surface (two carriers instead of one) in exchange for a materially stronger, better-paced foreshadow ahead of a CRITICAL Act III reveal.

**Recommended solution:** adopt combined C1+C3 as an `AI_PROPOSED_CANDIDATE` / `TEST_ONLY` annotation in this pilot workspace. Do **not** write it into `ACT_I_BEAT_SHEET.md`'s real DOWNSTREAM REFERENCES line yet — that would be a durable-state write outside this run's RUN_MODE. If the author later wants this promoted, it re-enters through the normal Beat-Sheet-adjacent annotation update once Scene layer opens, not through this pilot.

**Why:** cheapest available fix (zero new beats/events), fully reversible, respects every locked design (CDL-074/085) and the current Director Layer Gate state.

**What is sacrificed by choosing it over doing nothing:** nothing measurable — the alternative (leaving P-A05 at 2 touches) risks a colder Act III reveal; this candidate's cost is two short annotation notes, not committed content.

**Director Decision found:** **NONE.** This resolves entirely within `AI_PROPOSED_CANDIDATE` territory (see Question Funnel verdict, `02_question_funnel.md`).

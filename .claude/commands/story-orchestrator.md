Use the `story-orchestrator` skill as the single entry point.

Request:
$ARGUMENTS

The orchestrator must recover sources, diagnose prerequisites, select and sequence specialist skills, scale depth to the task, execute useful upstream work, label completion status, and suggest the next highest-value work. The user should not need to name individual skills.
